# Production ortamında (NODE_ENV=production) npm devDependencies'i atlar.
# Derleme typescript, vite, tailwind ve @vitejs/plugin-react gibi dev araçlarına
# ihtiyaç duyduğu için bunlar her koşulda kurulmalıdır.
# `include`, NODE_ENV'den türeyen `omit` ayarına göre önceliklidir.
include=dev
